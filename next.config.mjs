/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "flowbite.com",
      "source.unsplash.com",
      "images.unsplash.com",
      "images.domains",
      "images.remotePatterns",
      "storage.googleapis.com",
    ],
  },
};

export default nextConfig;
